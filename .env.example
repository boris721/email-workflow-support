# ─────────────────────────────────────────────
# Email Support Workflow — Environment Config
# ─────────────────────────────────────────────
# Copy this file to .env and fill in your values.

# ── Service Identity ──────────────────────────
# Display name for your product/service (used in logs and signatures)
SERVICE_NAME="My SaaS Support"

# ── IMAP Configuration ────────────────────────
# Incoming mail server (to fetch support emails)
IMAP_HOST=imap.example.com
IMAP_PORT=993

# ── SMTP Configuration ────────────────────────
# Outgoing mail server (to send approved replies)
SMTP_HOST=smtp.example.com
SMTP_PORT=465

# Display name for outgoing emails (e.g., "Acme Support")
# Defaults to SERVICE_NAME if not set.
# SMTP_FROM_NAME=Acme Support

# ── Email Credentials ─────────────────────────
# Used for both IMAP and SMTP authentication
EMAIL_USER=support@example.com
EMAIL_PASS=your_app_password_here

# ── OpenClaw Gateway (LLM Access) ─────────────
# The workflow calls OpenClaw's llm-task tool via HTTP.
# Make sure the gateway is running: openclaw gateway start
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_GATEWAY_TOKEN=your_gateway_token_here

# Alternative token variable name (either works):
# CLAWD_TOKEN=your_token_here

# ── Paths ─────────────────────────────────────
# Path to your reference responses knowledge base
REFERENCES_FILE=./data/reference-responses.json

# ── Discord Notifications ─────────────────────
# Channel ID where draft previews are posted for review.
# Leave empty to disable Discord notifications.
DISCORD_CHANNEL_ID=
